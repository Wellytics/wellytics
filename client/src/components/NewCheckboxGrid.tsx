import React, { FC } from 'react'
import { NewQuestionProps } from '../routes/dashboard/new'

export const NewCheckboxGrid: FC<NewQuestionProps> = () => {
  return (
    <div>NewCheckboxGrid</div>
  )
}
